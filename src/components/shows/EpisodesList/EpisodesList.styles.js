import styled from 'styled-components';
import { media } from '../../../styles/theme/theme';

export const EpisodesListWrapper = styled.div`
  padding-block: 30px;

  ${media.lg`
      padding-block:20px;
   `};
`;
